- title "#{@constituency.name} - #{t('.title', members: t('members'))} #{t('past_present')}"

%h1= "#{@constituency.name} - #{t('.title', members: t('members'))} #{t('past_present')}"
= render 'constituencies/constituency'
